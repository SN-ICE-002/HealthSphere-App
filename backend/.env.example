PORT=5000
NODE_ENV=development
JWT_SECRET=your_jwt_secret_here
CLIENT_URL=http://localhost:3000

# Database Configuration (Choice 1: Using separate fields)
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=healthsphere_DB

# Database Configuration (Choice 2: Using connection string - recommended for production)
# DATABASE_URL=postgres://user:password@host:port/database
